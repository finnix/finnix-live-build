#!/bin/sh

set -e

# Do not remove man cache
# https://github.com/finnix/finnix/issues/9
#rm -rf /var/cache/man/*

for _DIRECTORY in /tmp /var/tmp
do
	rm -rf ${_DIRECTORY}

	mkdir -p ${_DIRECTORY}
	chmod 1777 ${_DIRECTORY}
done
